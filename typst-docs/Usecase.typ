#import "../styles.typ": *

#doc-title("Use Case Specification", subtitle: "Описание прецедента")

#section("1.")[Use-Case Name (Название прецедента)]

#placeholder[Укажите название прецедента.]

#section("2.")[Actors (Акторы)]

#placeholder[Укажите основное действующее лицо прецедента и дополнительных (если они есть)]

#section("2.")[Brief Description (Краткое описание)]

#placeholder[Кратко опишите прецедент, с чего он начинается.]

#section("3.")[Flow of Events (Последовательность событий)]

#subsection("3.1")[Basic Flow (Главная последовательность)]

#placeholder[Опишите последовательность шагов, которые выполняет пользователь. Описание должно быть максимально абстрактным, без привязки к элементам интерфейса и вводу определенных данных.]

#subsection("3.2")[Alternative Flows (Альтернативные последовательности)]

#placeholder[Опишите альтернативные последовательности шагов, которые также приводят к успешному выполнению прецедента.]

#section("4.")[Preconditions (Предусловия)]

#placeholder[Укажите условия, которые должны выполняться, чтобы прецедент начался.]

#section("5.")[Postconditions (Постусловия)]

#placeholder[Укажите, что изменится в системе после выполнения прецедента.]

#section("6.")[Extension Points (Точки расширения)]

#placeholder[Если прецедент содержит вложенные прецеденты, которые добавляют к нему новые шаги, укажите здесь ссылку на них, на каком шаге главной последовательности они включаются.]

#section("7.")[Use-case diagram (Диаграмма прецедента)]

#placeholder[UML диаграмма данного прецедента]

#section("8.")[Interface example (Пример интерфейса)]

#placeholder[Макет пользовательского интерфейса, который будет использоваться для выполнения прецедента]
